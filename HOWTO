# vytvoření
git init
git add .
git commit -m "Initial commit"
git remote add libmirisdr-2 http://code.google.com/p/libmirisdr-2/
git push libmirisdr-2 master

# doplnění
git add *
git commit -m 'co se změnilo'
git push libmirisdr-2 master
